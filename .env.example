# ============================================================================
# ASL Real-time Recognition - Environment Configuration
# ============================================================================
# Copy this file to .env and adjust values as needed
# Usage: export $(cat .env | xargs) && uvicorn app.server:app
# ============================================================================

# ----------------------------------------------------------------------------
# Model Configuration
# ----------------------------------------------------------------------------
MODEL_PATH=./model/WLASL20c_model.h5
USE_DUMMY_MODEL=0

# ----------------------------------------------------------------------------
# TensorFlow Performance Tuning
# ----------------------------------------------------------------------------
# Number of threads for TensorFlow operations
# Lower values = more stable, higher = faster (if you have CPUs)
TF_INTER_OP_THREADS=1
TF_INTRA_OP_THREADS=2

# TensorFlow logging level (0=all, 1=info, 2=warning, 3=error)
TF_CPP_MIN_LOG_LEVEL=2

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------
# Logging level: debug, info, warning, error
LOG_LEVEL=info

# Maximum concurrent clients (prevent server overload)
MAX_CLIENTS=50

# CORS origins (comma-separated, use * for all)
CORS_ORIGINS=*

# ----------------------------------------------------------------------------
# Socket.IO Configuration
# ----------------------------------------------------------------------------
# Ping timeout in seconds (disconnect if no response)
SOCKETIO_PING_TIMEOUT=60

# Ping interval in seconds (how often to check connection)
SOCKETIO_PING_INTERVAL=25

# ============================================================================
# Production Recommended Settings
# ============================================================================
# For stable production deployment on cloud platforms:
#
# MODEL_PATH=./model/WLASL20c_model.h5
# USE_DUMMY_MODEL=0
# TF_INTER_OP_THREADS=1
# TF_INTRA_OP_THREADS=2
# TF_CPP_MIN_LOG_LEVEL=2
# LOG_LEVEL=info
# MAX_CLIENTS=50
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# SOCKETIO_PING_TIMEOUT=60
# SOCKETIO_PING_INTERVAL=25
#
# ============================================================================

# ============================================================================
# Development Settings
# ============================================================================
# For local testing with debug output:
#
# LOG_LEVEL=debug
# MAX_CLIENTS=5
# CORS_ORIGINS=*
#
# ============================================================================

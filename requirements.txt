# ============================================================================
# ASL Real-time Recognition - Production Requirements
# ============================================================================
# Python 3.7+
# Updated: 2025-11-10
# Purpose: Minimal production-ready dependencies for Socket.IO + TensorFlow backend
# ============================================================================

# ----------------------------------------------------------------------------
# Core Web Framework & API
# ----------------------------------------------------------------------------
fastapi==0.88.0
uvicorn[standard]==0.20.0
pydantic==1.10.13

# ----------------------------------------------------------------------------
# Real-time Communication (Socket.IO)
# ----------------------------------------------------------------------------
python-socketio==5.8.0
python-engineio==4.8.0

# ----------------------------------------------------------------------------
# Machine Learning & Deep Learning
# ----------------------------------------------------------------------------
tensorflow==2.3.1
h5py==2.10.0
numpy==1.18.2

# ----------------------------------------------------------------------------
# Image Processing
# ----------------------------------------------------------------------------
# Using headless version for server deployment (no GUI dependencies)
opencv-python-headless==4.5.5.64

# ----------------------------------------------------------------------------
# Optional: Production Monitoring & Logging (uncomment if needed)
# ----------------------------------------------------------------------------
# python-json-logger==2.0.4
# sentry-sdk==1.34.0

# ============================================================================
# Development Dependencies (install separately for dev/test)
# ============================================================================
# pytest==7.4.0
# pytest-asyncio==0.21.0
# black==23.7.0
# flake8==6.1.0

# ============================================================================
# Notes:
# ============================================================================
# 1. TensorFlow 2.3.1 is pinned to match your trained model
# 2. opencv-python-headless is used instead of opencv-python to reduce
#    dependencies and Docker image size (no Qt/GTK requirements)
# 3. uvicorn[standard] includes websockets support needed for Socket.IO
# 4. All Jupyter/notebook dependencies removed (not needed in production)
# 5. Removed: Flask, matplotlib, pandas, scikit-learn (not used in runtime)
# ============================================================================
